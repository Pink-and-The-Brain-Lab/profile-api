import "reflect-metadata";
import express from "express";
import routes from "./routes";
import './database';
import cors from 'cors';
import { clientErrorHandle } from "millez-lib-api";
import { VALIDATE_TOKEN } from "./constants/validate-token";
const app = express();
app.use(express.json());
app.use(cors());
app.use(VALIDATE_TOKEN.validate);
app.use(clientErrorHandle);
app.use(routes);
export default app;
